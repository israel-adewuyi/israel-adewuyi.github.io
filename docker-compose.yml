# this file builds a local image that matches Gemfile.lock
services:
  jekyll:
    image: israel-adewuyi-blog:local
    build: .
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
